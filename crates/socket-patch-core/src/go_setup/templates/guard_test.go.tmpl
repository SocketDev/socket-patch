// Code generated by `socket-patch setup`. DO NOT EDIT.
package socketpatchguard

import "testing"

// TestSocketPatchesApplied fails — after first attempting to self-heal via
// `socket-patch apply` — when the committed go.mod `replace` copies under
// .socket/go-patches/ have drifted from .socket/manifest.json (a stray `go mod`
// change, an un-re-applied dependency bump, or a stale manifest).
//
// CAVEAT: `go test` caches results by package source, NOT by the external
// .socket/ files this test reads, so a WARM cache can serve a stale PASS after
// drift. For a reliable merge gate run the cache-immune check directly in CI:
//
//	socket-patch apply --check --ecosystems golang
//
// (or `go test -count=1 ./...`). This test is the cold-cache convenience gate;
// the always-on runtime gate is this package's init() (fires on every run).
func TestSocketPatchesApplied(t *testing.T) {
	if msg, fail := check(); fail {
		t.Fatal(msg)
	}
}
