// Code generated by `socket-patch setup`. DO NOT EDIT.
package socketpatchguard

import "testing"

// TestSocketPatchesApplied fails — after first attempting to self-heal via
// `socket-patch apply` — when the committed go.mod `replace` copies under
// .socket/go-patches/ have drifted from .socket/manifest.json (a stray `go mod`
// change, an un-re-applied dependency bump, or a stale manifest). Make
// `go test ./...` a required CI step.
//
// check() reads go.mod, the manifest, and the committed copies IN-PROCESS, so
// `go test`'s result cache treats them as inputs and re-runs this gate whenever
// any of them change — it will NOT serve a stale PASS on a warm cache after
// drift. (The complementary always-on gate is this package's init(), which
// fires on every `go run`/binary launch.)
func TestSocketPatchesApplied(t *testing.T) {
	if msg, fail := check(); fail {
		t.Fatal(msg)
	}
}
