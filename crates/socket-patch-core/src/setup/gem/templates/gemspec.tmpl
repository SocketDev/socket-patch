# Code generated by `socket-patch setup`. DO NOT EDIT.
#
# Minimal gemspec for the in-tree socket-patch Bundler plugin. `setup` references
# it from the Gemfile via `plugin "socket-patch", path: File.expand_path(...)`;
# the directory must be committed so clones and CI have the plugin on disk.
# (Phase 2 replaces this in-tree plugin with a published `socket-patch-bundler`
# gem.)
Gem::Specification.new do |s|
  s.name        = "socket-patch"
  s.version     = "0.0.0"
  s.summary     = "Bundler plugin that keeps socket-patch gem patches applied on every bundle install."
  s.description = s.summary
  s.authors     = ["Socket"]
  s.license     = "MIT"
  s.files       = ["plugins.rb"]
  # The plugin dir is flat (plugins.rb at the root, no lib/). Bundler refuses
  # to load a plugin whose require paths are missing on disk ("The following
  # plugin paths don't exist: .../lib ... Continuing without installing
  # plugin"), so the default `lib` must be overridden.
  s.require_paths = ["."]
  s.required_ruby_version = ">= 2.6.0"
end
